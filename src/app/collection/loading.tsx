export default function CollectionLoading() {
  return (
    <div style={{ minHeight: "100vh", background: "#FDFCF8" }}>
      {/* Nav placeholder — same height as AppNav so content doesn't jump */}
      <div style={{ height: 56, borderBottom: "1px solid #e8e8e2", background: "#FDFCF8" }} />
    </div>
  );
}
